// diary.h

#ifndef DIARY_H
#define DIARY_H

#include <stdio.h>    
#include <stdlib.h>   
#include <string.h>   

// Function Declarations
void displayMenu();     // Displays the main menu options
void addEntry();        // Adds a new diary entry
void viewEntries();     // Displays all diary entries
void searchEntry();     // Searches for a specific entry by date
void editEntry();       // Edits an existing diary entry
void deleteEntry();     // Deletes a diary entry
void encryptFile();     // Encrypts the diary file
void decryptFile();     // Decrypts the diary file
void backupDiary();     // Creates a backup of the diary file

#endif



